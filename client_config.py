# ! Essential for Client to run.
import os
TOKEN: str = f'{os.environ["TOKEN"]}'  # ? Find this on your discord developer portal
PREFIX: str = f'ex.'  # ? Prefix for all Non-Slash Commands.
SLASH_MODE: bool = True  # ? I set this to False when doing lots of testing
